rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /profiles/{profile} {
      allow read, write: if request.auth!=null;
      // allow write: if request.auth!=null;
      // allow read: if request.auth.uid == profile.name;
    }
  }
}
